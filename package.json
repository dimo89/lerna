{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.0.0-rc.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "postinstall": "npm run bootstrap",
    "start": "lerna run start --stream --scope core",
    "prod": "lerna run prod --stream"
  }
}
